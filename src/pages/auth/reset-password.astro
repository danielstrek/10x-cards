---
import Layout from "../../layouts/Layout.astro";
import ResetPasswordForm from "../../components/auth/ResetPasswordForm";

// This page is accessed via email link with reset token
// Token is automatically handled by Supabase SSR (in cookies)
// No need to check if user is logged in - token validates the session
---

<Layout title="Ustaw nowe hasło - 10x Cards">
  <ResetPasswordForm client:load />
</Layout>
